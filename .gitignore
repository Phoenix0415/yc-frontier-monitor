# generated / machine state — rebuilt by `python3 scripts/yc.py update|build`
# (data/companies.json and data/changelog.json ARE tracked: the GitHub Action
# needs the previous state to diff against, and the history is the point)
data/snapshots/
data/auto.log
dist/
site/data.js
__pycache__/
*.pyc
.DS_Store
.claude/settings.local.json

# local-only planning docs — SPEC.md + enrichment-plan.md are private roadmap,
# deliberately kept out of the public repo (owner decision, 2026-06-13)
specs/

# secrets — API keys etc. NEVER commit these (repo is public)
.env
.env.*
!.env.example
*.key
